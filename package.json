{
  "name": "op-band-control",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "sh scripts/package.sh",
    "check": "sh scripts/validate.sh",
    "test": "node --test tests/*.test.mjs",
    "serve": "python3 -m http.server 4173 --bind 127.0.0.1 --directory webroot"
  },
  "engines": {
    "node": ">=20"
  }
}
